extends layout
block content
  br
  if submissions
    form(method='post')
      each submission in submissions
        div 
          input(type='radio' name='selection' value='#{submission.filename}')
          strong Lab: #{submission.labname}
          div(class='labAttributes')
            div Upload Date: #{submission.date}
            div Output: #{submission.stdout}
            div Correct:
      input(type='submit' value='Download')