extends layout

block content
  br
  form(class='bootstrap-frm' method='post', enctype='multipart/form-data')
    h1 Lab Uploader
    select(name='lab_name')
      option(value="") Lab Name
      each lab in labs
        option(value='#{lab.name}')=lab.name
    input(type='file', name='file')
    input(type='submit')
    br
    br
    div #{message} #{output}
